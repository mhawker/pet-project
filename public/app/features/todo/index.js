/*jslint browser */
/*global window */

/**
 * Load the to-do feature
 */
(function (define) {
    "use strict";
    define([
        "./todo-ctrl",
        "./todo-service",
        "./todo-route"
    ], function () {
        return undefined;
    });
}(window.define));
