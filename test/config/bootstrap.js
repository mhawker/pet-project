/*jslint browser */
/*global window */

/**
 * bootstraps angular onto the window.document node
 */
(function (define) {
    "use strict";
    define([
        "angular",
        "angularResource",
        "angularRoute",
        "angularMocks"
    ], function () {
        return undefined;
    });
}(window.define));
