/*jslint browser */
/*global window */

/**
 * Load all directives
 */
(function (define) {
    "use strict";
    define([
        "./todo-escape",
        "./todo-focus"
    ], function () {
        return undefined;
    });
}(window.define));
